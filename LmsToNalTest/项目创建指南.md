# LmsToNalTest 项目创建指南

## 📋 步骤 1: 在 Xcode 中创建新项目

1. 打开 Xcode
2. 选择 `File` → `New` → `Project...`
3. 选择 `iOS` → `Unit Testing Bundle`
4. 点击 `Next`
5. 配置项目：
   - **Product Name**: `LmsToNalTest`
   - **Organization Identifier**: `com.yourcompany` (或你的标识符)
   - **Language**: `Objective-C`
   - **Use Core Data**: 不勾选
   - **Include Unit Tests**: 已勾选（默认）
6. 点击 `Next`
7. **重要**: 选择 `LmsToNalTest` 文件夹作为保存位置
8. 点击 `Create`

## 📋 步骤 2: 复制库文件

### 方法 1: 使用 Finder/文件管理器
1. 打开 `DCTestUniPlugin/DCTestUniPlugin/NAL_NL2_SDK` 文件夹
2. 复制整个 `NAL_NL2_SDK` 文件夹
3. 粘贴到 `LmsToNalTest/LmsToNalTest/` 目录下

### 方法 2: 使用终端
```bash
cd LmsToNalTest/LmsToNalTest
cp -R ../../DCTestUniPlugin/DCTestUniPlugin/NAL_NL2_SDK .
```

## 📋 步骤 3: 添加文件到项目

1. 在 Xcode 项目导航器中，右键点击 `LmsToNalTest` 组
2. 选择 `Add Files to "LmsToNalTest"...`
3. 添加以下文件：
   - `TestModule.h`
   - `TestModule.mm`
   - `NAL_NL2_SDK` 文件夹（选择 "Create groups" 和 "Add to targets: LmsToNalTest"）

4. 在测试 target (`LmsToNalTestTests`) 中：
   - 右键点击 `LmsToNalTestTests` 组
   - 选择 `Add Files to "LmsToNalTest"...`
   - 添加 `LmsToNalTestTests.m`（如果还没有）

## 📋 步骤 4: 配置 Build Settings

### 4.1 选择主 Target (LmsToNalTest)

1. 点击项目名称（最顶部）
2. 选择 `LmsToNalTest` target
3. 切换到 `Build Settings` 标签页
4. 搜索并配置以下设置：

**Framework Search Paths:**
```
$(PROJECT_DIR)/LmsToNalTest/NAL_NL2_SDK
```

**Header Search Paths:**
```
$(PROJECT_DIR)/LmsToNalTest
```

**Other Linker Flags:**
```
-framework NAL_NL2
```

### 4.2 配置 Build Phases

1. 切换到 `Build Phases` 标签页
2. 展开 `Link Binary With Libraries`
3. 点击 `+` 按钮
4. 添加 `NAL_NL2.framework`（从项目中选择）

### 4.3 配置测试 Target (LmsToNalTestTests)

1. 选择 `LmsToNalTestTests` target
2. 在 `Build Settings` 中配置相同的路径
3. 在 `Build Phases` → `Link Binary With Libraries` 中添加：
   - `LmsToNalTest.framework`（主项目）
   - `NAL_NL2.framework`

4. 在 `Build Phases` → `Dependencies` 中添加：
   - `LmsToNalTest` target

## 📋 步骤 5: 配置 Info.plist

确保测试 target 有正确的 Info.plist：
- 路径: `LmsToNalTestTests/Info.plist`
- 如果不存在，创建一个简单的 Info.plist

## 📋 步骤 6: 运行测试

1. 选择 Scheme: `LmsToNalTest` 或 `LmsToNalTestTests`
2. 按 `⌘ + U` 运行测试
3. 查看控制台输出，会显示详细的测试信息

## ✅ 验证配置

如果一切配置正确，你应该能够：
1. 成功编译项目（无错误）
2. 运行测试并看到详细的输出
3. 在控制台看到测试结果和调试信息

## 🐛 常见问题

### 问题 1: 找不到 NAL_NL2.framework
**解决**: 检查 Framework Search Paths 是否正确

### 问题 2: 找不到 TestModule.h
**解决**: 检查 Header Search Paths 和文件是否已添加到 target

### 问题 3: 链接错误
**解决**: 确保在 Link Binary With Libraries 中添加了所有必要的 framework

### 问题 4: 运行时崩溃
**解决**: 确保 NALNL2Bundle.bundle 已添加到 Copy Bundle Resources

## 📝 测试参数

测试使用的默认参数：
```json
{
    "age": 20,
    "gender": 1,
    "ac": [45, 55, 55, 55, 55, 55, 55, 55, 55, 55, 55, 55, 55, 55, 55, 55, 55, 55, 55, 55, 55, 55],
    "isLeft": false,
    "level": 80
}
```

可以在 `LmsToNalTestTests.m` 中修改这些参数进行不同的测试。

