# 快速开始

## 🚀 最简单的创建方式

### 方式 1: 使用 Xcode 模板（推荐）

1. **打开 Xcode**
2. **创建新项目**:
   - `File` → `New` → `Project...`
   - 选择 `iOS` → `Framework`
   - 命名为 `LmsToNalTest`
   - 保存到 `LmsToNalTest` 文件夹

3. **添加测试 Target**:
   - `File` → `New` → `Target...`
   - 选择 `Unit Testing Bundle`
   - 命名为 `LmsToNalTestTests`

4. **复制文件**:
   - 将 `TestModule.h` 和 `TestModule.mm` 添加到主 target
   - 将 `LmsToNalTestTests.m` 添加到测试 target
   - 复制 `NAL_NL2_SDK` 文件夹到项目目录

5. **配置路径**（在 Build Settings 中）:
   ```
   Framework Search Paths: $(PROJECT_DIR)/LmsToNalTest/NAL_NL2_SDK
   Header Search Paths: $(PROJECT_DIR)/LmsToNalTest
   ```

6. **链接 Framework**:
   - 在 Build Phases → Link Binary With Libraries 中添加 `NAL_NL2.framework`

7. **运行测试**: 按 `⌘ + U`

## 📦 已准备的文件

项目目录中已包含：
- ✅ `TestModule.h` - 头文件
- ✅ `TestModule.mm` - 实现文件（包含 lmsToNal 函数）
- ✅ `LmsToNalTestTests.m` - 测试代码
- ⚠️ `NAL_NL2_SDK` - 需要手动复制（见下方）

## 📂 需要手动复制的文件

由于路径限制，请手动复制以下文件：

**源路径**: `DCTestUniPlugin/DCTestUniPlugin/NAL_NL2_SDK`  
**目标路径**: `LmsToNalTest/LmsToNalTest/NAL_NL2_SDK`

### 复制方法

**Windows (PowerShell)**:
```powershell
Copy-Item -Path "DCTestUniPlugin\DCTestUniPlugin\NAL_NL2_SDK" -Destination "LmsToNalTest\LmsToNalTest\NAL_NL2_SDK" -Recurse
```

**macOS/Linux (Terminal)**:
```bash
cp -R DCTestUniPlugin/DCTestUniPlugin/NAL_NL2_SDK LmsToNalTest/LmsToNalTest/
```

## ✅ 验证

复制完成后，目录结构应该是：
```
LmsToNalTest/
├── LmsToNalTest/
│   ├── TestModule.h
│   ├── TestModule.mm
│   └── NAL_NL2_SDK/          ← 这个文件夹需要手动复制
│       ├── NAL_NL2.framework
│       └── NALNL2Bundle.bundle
└── LmsToNalTestTests/
    └── LmsToNalTestTests.m
```

## 🎯 下一步

1. 在 Xcode 中打开项目
2. 按照 `项目创建指南.md` 中的步骤配置项目
3. 运行测试！

